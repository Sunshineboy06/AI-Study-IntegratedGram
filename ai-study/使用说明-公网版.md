# AI学习一体化平台 · 公网部署版

面向**公网开放**的轻量部署版：服务器不做任何 AI 计算，访客用平台内置的固定提示词在自己的第三方 AI 里生成学习内容，回来上传导入即可。全套学习功能与单机版完全一致。

## 访客使用指引（三步）

1. **复制提示词**：「数据管理」页 → 「🧠 第三方 AI 生成文件提示词」，按需复制题库 / 大纲 / 单词表三份提示词之一
2. **第三方 AI 生成**：粘贴到 Kimi、ChatGPT、豆包等任意 AI，把末尾「素材」替换为自己的笔记内容，AI 会输出平台固定格式的文件
3. **上传导入**：回到「数据管理」上传生成的 .json / .md / .txt 文件，自动校验并导入，之后即可刷题、看脑图、背单词

> 防重复：词库中已有的单词自动跳过并保留旧数据；题库重复导入请先删除旧题库。

## 手机 / 平板使用注意

- **数据不跨设备同步**：所有数据（词库、刷题记录）只存在各自设备的浏览器里。电脑上导入的单词，手机上要重新导入一次；手机端词库为空时，「背单词」页会出现引导卡，可一键试用 10 个内置示例词。
- **单词朗读**：请用 **Edge 或 Chrome** 打开本站。微信 / QQ 内置浏览器和国产 ROM 自带浏览器普遍缺少英文语音引擎，朗读会无声（页面会给出提示）。
- 想要更自然的读音，可在背单词设置里配置 **Azure 神经语音**（走网络合成，任何浏览器都能出声）。

## 管理员部署（Alibaba Cloud Linux 3）

见 `deploy-linux/README-部署.md`，一条命令完成部署。

## 与单机版的差异

| | 单机版 | 公网部署版 |
|---|---|---|
| AI 生成（服务端调用大模型） | ✅ | ❌（用第三方 AI + 提示词替代） |
| 服务器依赖 | flask + LLM 库 + API Key | 仅 flask + 文档解析 |
| 服务器配置要求 | 建议 2核4G | 2核2G 即可 |
| 用户数据 | 各自浏览器本地 | 各自浏览器本地（不变） |
